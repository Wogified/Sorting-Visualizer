import { bubbleSort } from './bubbleSort';
import { insertionSort } from './insertionSort';
import { genArr, shuffle, AnimateSort, stopAnimation } from './startingDataFunctions';

export { insertionSort, bubbleSort, genArr, shuffle, AnimateSort, stopAnimation };
